<% if errors.any? %>
	<div id="error_explanation">
		<h2><%=t :save_errors, :count => errors.count %></h2>
		<ul>
		<% errors.full_messages.each do |msg| %>
		  <li><%= msg %></li>
		<% end %>
		</ul>
	</div>
<% end %>